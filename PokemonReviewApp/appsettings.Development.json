{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Jwt": {
    // Local development only. This key is in source control, so treat every token it signs
    // as public. Any other environment must supply Jwt:Key through user-secrets or the
    // Jwt__Key environment variable.
    "Key": "dev-only-signing-key-change-me-32chars-minimum"
  },
  "Identity": {
    "AdminUser": {
      "UserName": "admin",
      "Email": "admin@localhost",
      "Password": "Admin123!"
    }
  }
}
